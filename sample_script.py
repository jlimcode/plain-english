def foobar(n: int) -> str:
    """Return the string "foobar" repeated n times.

    Args:
        n (int): The number of times to repeat the string "foobar".

    Returns:
        str: The string "foobar" repeated n times.
    """
    return "foobar" * n